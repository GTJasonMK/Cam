# Worker 基础配置
API_SERVER_URL=http://localhost:3000
WORKER_ID=worker-local
SUPPORTED_AGENTS=

# 可选：Worker 上报能力（仅上报“变量名”，不上传任何值）
# 用于服务端未配置密钥时，仍允许由本地常驻 Worker 使用本机环境执行任务
CAM_WORKER_REPORTED_ENV_VARS=OPENAI_API_KEY,ANTHROPIC_API_KEY,GITHUB_TOKEN

# 当 Web 启用认证后，需要配置以下 token（与 CAM_AUTH_TOKEN 一致）
API_AUTH_TOKEN=

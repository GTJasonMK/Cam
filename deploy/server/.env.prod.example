# ============================================================
# CAM 生产环境变量模板
# 复制为 deploy/server/.env.prod 后再填写真实值
# ============================================================

# ---- 基础域名与安全 ----
DOMAIN=cam.example.com
CAM_PUBLIC_BASE_URL=https://cam.example.com
CAM_COOKIE_DOMAIN=cam.example.com
CAM_COOKIE_SECURE=true
CAM_ALLOW_ANONYMOUS_ACCESS=false

# 访问令牌（用于 legacy 访问 & worker -> web 鉴权）
CAM_AUTH_TOKEN=replace-with-a-strong-random-token

# Secrets 加密主密钥（必须）
CAM_MASTER_KEY=replace-with-a-strong-master-key

# ---- 部署模式 ----
# host   — 宿主机直接运行（默认，构建快，适合快速迭代）
# docker — Docker Compose 部署（完整隔离）
CAM_DEPLOY_MODE=host

# ---- 数据目录（宿主机）----
CAM_DATA_DIR=/opt/cam/data
CAM_LOGS_DIR=/opt/cam/logs
CAM_REPOS_DIR=/opt/cam/repos

# ---- Agent / Git 凭据（按需填写）----
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GITHUB_TOKEN=
GITLAB_TOKEN=
GITEA_TOKEN=
CAM_GIT_PROVIDER=

# ---- OAuth（按需填写）----
CAM_OAUTH_STATE_SECRET=
CAM_OAUTH_GITHUB_CLIENT_ID=
CAM_OAUTH_GITHUB_CLIENT_SECRET=
CAM_OAUTH_GITLAB_CLIENT_ID=
CAM_OAUTH_GITLAB_CLIENT_SECRET=
CAM_OAUTH_GITLAB_BASE_URL=https://gitlab.com

# ---- Webhook（按需填写）----
CAM_WEBHOOK_URL=
CAM_WEBHOOK_URLS=
CAM_WEBHOOK_TOKEN=
CAM_WEBHOOK_TIMEOUT_MS=4000
CAM_WEBHOOK_PROVIDER=generic
CAM_WEBHOOK_EVENTS=
CAM_WEBHOOK_PROGRESS_STATUSES=running,awaiting_review,completed,failed,cancelled

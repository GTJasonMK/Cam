添加多Claude code管理多codex完成多个子任务的功能
添加一个步骤可以并行使用多个agent共同完成的功能
步骤间agent的通信机制由.conversations\step[]中的文件完成，每个步骤的输出文档都必须记录在对应的步骤的目录中，下一个步骤的agent会使用上一个步骤的目录中的指定文件作为输入，步骤内agent的通信也是通过.conversations\step[]中的文件完成，但是输出都会存在这个目录，总而言之就是一个步骤的输出文件都放在同一个目录，不同步骤的agent会以上一个步骤的目录中的某些文件来作为输入来辅助完成步骤的功能
当前的模板（除流水线模板之外）其实就是提示词，所以我觉得改成提示词会好一点，然后就是流水线应该单独作为一个页面来进行编辑，我想改成可视化的节点编辑，每个节点需要配置agent以及每个agent的提示词模板（就是上面提到的），还需要配置输入、输入条件等等